# Copy to .env and fill in. EXPO_PUBLIC_* vars are inlined into the app bundle
# at build time. The backend brokers the whole OAuth flow (it holds the Google
# client id + secret), so the app only needs to know where the backend is.

# FitVibe Go backend base URL (no trailing slash).
EXPO_PUBLIC_API_BASE_URL=http://localhost:8080
